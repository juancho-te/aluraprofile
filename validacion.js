//Haz tú validación en javascript acá

        //alert("La resolución de tu pantalla es: " + screen.width + " x " + screen.height);
     function valida (email){
        let emailText=email;
        if (emailText.indexOf(".com") == -1)
        alert("error"+emailText);
        boton.setFocus();

    }
